import { z } from 'zod';
import { createRouter } from '../context';
import { publicProcedure } from '../procedure';
import { sophomoreParticipant } from '$lib/airtable-api';

export const userRouters = createRouter({
    
})